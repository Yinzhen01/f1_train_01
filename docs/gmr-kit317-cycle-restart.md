# Cycle 三组中断恢复

2026-09-12 用户要求重新启动之前三组训练。原任务 164/165/166 为状态6，
控制台分别止于8549/8564/8446；终止根因尚未确认，没有余额不足的有效证据。
保持旧任务及中间模型，不修改轻擦惩罚分支，不改奖励、动力学、观测或参考。
仅在现有 cycle 分支增加独立恢复入口 `train_gmr_cycle_restart.py`，默认旧入口行为保留。

| 组 | 恢复任务/模型 | 文件内 iter | 下一次更新 | 到9000尚需 |
| --- | --- | --- | --- | --- |
| contact | TASK_20260912_164/model_8500 | 8500 | 8501 | 499 |
| smooth | TASK_20260912_165/model_8500 | 8500 | 8501 | 499 |
| both | TASK_20260912_166/model_8400 | 8400 | 8401 | 599 |

原训练代码 bee194f，三份完整文件SHA256绑定在 `humanoid/gmr_cycle_restart.py`。
周期 checkpoint 在标记的更新之后保存，不能套用最终 model9000 内部 iter8999 的规则，
也不能把8500再训练一遍。断点之后未保存的更新不能恢复。

恢复全部33个网络张量、PPO Adam及状态估计器优化器的已保存状态。
实际PPO Adam有33份动量，接触/平滑组计数4008，组合组3208；
独立状态估计器优化器 state 为空，按原样保留（当前代码由主优化器更新全部参数），
不得把空state误称为缺失或宣称其有历史动量。
固定lr1e-5、seed5，正式4096环境。仿真、观测历史、随机数状态未保存，
重新初始化；不声称逐位连续，也不声称中断恢复结果等同从未中断。

共享恢复代码先用组合组原model8400做512环境×20更新短测，目标model8421，
通过后正式三组仍分别从表中的原中间模型开始，不从短测模型继续。
每组正式终点model9000，恢复网络/优化器身份、有效配置和源任务回读必须匹配。
仅使用1×4090D 24GB/组，镜像保持V000124；账号身份与停用清单在调用前检查。
不创建监控、不扩大训练预算、不自动替换部署策略。

训练结束的1kHz成对诊断标记为 `restart8500` / `restart8400` 与 `trained`，
是“本次恢复之前/之后”的比较，不再误标成原model8000。
若要比较全部三组从原model8000的改善，需要另做同协议原模型评估。

本地三份真实checkpoint严格加载与有限值前向通过；全部优化器字段逐项相同。
新增7项测试覆盖身份、预算、两优化器恢复、计数连续性、缺失/非有限值与越组拒绝。
云端短测及正式启动状态以 outputs/gradmotion-cycle/restart 下记录为准。
