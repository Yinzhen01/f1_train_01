# AMP动态风格续训：第3–5轮

用户授权自动实验最多20轮。第1、2轮结果见amp-style-recovery.md；本分支从第二组
TASK_20260924_060的精确model_1000恢复，保持无DR、无观测噪声与nominal动力学。
不改变原数据、URDF、PD、动作尺度、观测字段/历史、39维AMP协议或10帧100Hz窗口。

## 证据与假设

第一组059缺失的1000回放由只读推理063补齐，模型权重与原评估逐项相等。
16条参考初态均完成20秒：059体前向速度0.5218m/s、足滑0.08349m/s、位置差分
加速度RMS均值92.80rad/s²；060对应0.3816、0.17171、86.02。两组各有3/16静止
起步失败。060不是整体最优；选它为共同起点是保留冻结姿态负样本、对动态问题
做受控诊断，059仍是较低足滑的比较基线。已有指标均不能放行DR或代表Sim2Real。

060示例的示范最近窗口误差主要来自dq和根角速度，不是静态关键点位置。
原动作差分系数-.002较弱，航向仅惩罚角速度，20秒可累计漂移；净接触力没有接触对
身份。新约束针对这些问题，不引入逐帧关节追踪或预设步态时钟。

## 匹配三组（已通过云端短测，正式续训已启动）

| 轮次 | group | 环境奖励 | AMP style_weight |
| --- | --- | --- | --- |
| 3/20 | refine_control | 原recovery_static奖励 | 1 |
| 4/20 | refine_smooth | 下列动态正则组合 | 1 |
| 5/20 | refine_noamp | 与refine_smooth完全相同 | 0 |

三组共同从同一checkpoint恢复Actor、Critic、状态估计器、D、全部优化器与50000
真实策略窗口；统一将PPO/状态估计优化器学习率设为5e-5，D超参保持原值。
仿真/RNG从seed5重新开始，只初始化真实新状态，不伪造观测/AMP历史；不是逐位
连续恢复旧仿真。smoke32环境×10新增更新；正式4096×500新增更新，累计到1500。
smoke权重不作正式源，正式仍从原1000开始。独立评估在累计1250、1500，各16环境、
静止/参考初态20秒；noamp仍训练D作旁路诊断，但其style不进入PPO奖励。
日志必须区分raw style_reward与weighted_style_reward；noamp后者严格为0。

动态正则仅在smooth/noamp启用：

- 动作一阶/二阶变化项系数-.002→-.02。
- 原yaw角速度误差平方系数-.1→-1；另加-.5*(1-cos(yaw))，约束朝世界+X。
  原Actor已经观察完整RPY，无新增不可观测目标。参考初始yaw约-0.47°到12.72°。
- 加速度连续鲁棒成本-.5*mean(2*(sqrt(1+(a/100)^2)-1))，a取100Hz原生dq差分。
  小误差近二次、大冲击线性尾，避免少量触地冲击主导；稳定支撑和摆动都受约束，
  没有不受罚的相位空档。前两个重置步屏蔽，本来已有全局dof_acc安全项保留。
- 足滑成本-2*mean(w*||v_sole_xy||²)。w使用Fn正部/(Fn正部+5)，0到5N无死区；
  精确碰撞mesh最低点距地0–1cm时几何权重1，1–2cm线性归零，拒绝高处自接触。
  凸包仅减少最低点投影计算量（测试保持原mesh投影极值），不修改物理碰撞资产。
  速度取底面顶点质心的刚体点速度，是足底代表点，不声称求解器真实接触点。

此为组合改进，不把control与smooth差异单独归因到一个新项。
smooth与noamp是仅style_weight不同的对照，用以检查继续使用AMP的增量作用；
因双方都从AMP预训练模型开始，它不是从零有/无AMP的因果证明。

## 身份与验证

源SHA256：def5eea837a60bf8bc4b7c3a481e8c3e84b5da3843e9af4d60b1e3c134c5dd63。
首次短测065/066/067在恢复前被文件哈希门禁拦截，没有进入PPO；旧eval对象可下载，
但平台挂载未返回预期内容。改用平台当前model1000对象，不绕过校验：本地逐项核对
final与原eval文件，159个张量、Actor/Critic/D/全部优化器/replay/迭代及身份完全相等，
只有infos不同。证据outputs/amp-refine/source060-equivalence.json，原eval不覆盖。
门禁绑定源身份/1000更新、恢复状态、数据/资产/特征指纹、无DR、匹配真实smoke。
跨平台归一化重算仅允许1e-6绝对/1e-5相对数值误差，D内mean/std仍逐位保留源权重。
本地测试包括真实源模型恢复；尚不能代表Gym运行通过。
评估模型编号8800000+更新数，回放包9900000+更新数，避免平台同编号覆盖。
普通model1500与精确评估model8801500须按manifest的SHA绑定，不混用名字推断身份。

验收继续要求持续前进、合理腿部交替、静止起步成功、足滑/抖动/方向改善及AMP
风格独立指标。停止只看D loss/奖励上升作为成功依据，所有组DR锁定false。

离线统一评估新增航向漂移/误差、速度10Hz以上功率占比、最近示范窗口各特征组
误差及可选固定源D审计。同一冻结D可跨组比较，但仍有源策略偏差，不是概率或
独立质量裁判；同场景存活/速度/抖动/足滑和最近窗口距离须一并报告。

## 云端短测记录（2026-09-24）

最终短测代码43e596d，训练实现指纹8052bc59c3bffd3178596c6660f7ec9aa7b078a8ca441e24b93e23230061117c。
control TASK_20260924_068、smooth 069、noamp 070，均正常完成1000→1010，32环境。
159个源训练张量相等的证据、三份运行manifest、模型和真实1秒×2环境×2起步方式
轨迹均已下载，门禁与SHA、无DR/噪声、100Hz控制、FK、reset历史、学习更新均复核。
noamp日志1001–1010的weighted_style_reward严格0；另两组严格等于raw style。
该1秒推理只测试管线，不是步态质量验收；formal从原1000恢复，不用1010。
可移植证书在docs/validation/refine_{control,smooth,noamp}_cloud_smoke.json。

统一离线基线（reference起步16条，排除前2秒，固定用060源D审计）：

| 指标 | 059 model1000 | 060 model1000 |
| --- | ---: | ---: |
| 存活20秒 | 16/16 | 16/16 |
| 前进速度m/s | 0.5218 | 0.3816 |
| 接触足底代表点滑速m/s | 0.08349 | 0.17171 |
| 位置差分关节加速度RMS rad/s² | 92.80 | 86.02 |
| 朝世界X航向RMS度 | 42.19 | 70.65 |
| 原生关节速度大于10Hz功率比例 | 0.11055 | 0.07838 |
| 最近示范窗口RMS zscore | 1.01450 | 1.05793 |
| 同一固定D归一化风格分（非概率） | 0.65824 | 0.14916 |

这是诊断对照，不用单项得分选优；低频动作速度/方向不同也影响加速度与频谱。
本地输出outputs/amp-refine/baseline-{A,B}-1000保留32条初态的全部分项和失败轨迹。

## 正式续训启动快照（2026-09-24 15:05，北京时间）

| 轮次 | 组 | Gradmotion任务 | 启动时间 | 已核验实际更新 |
| --- | --- | --- | --- | ---: |
| 3/20 | control | TASK_20260924_071 | 15:04:00 | 1024 |
| 4/20 | smooth + AMP | TASK_20260924_072 | 15:04:03 | 1024 |
| 5/20 | smooth，无AMP风格奖励 | TASK_20260924_073 | 15:04:07 | 1022 |

代码db79136e558c833199d1fdc6b56f2d605f58cca2；每组4096环境、1×4090D/ESKU000001、
source060 model1000（def5eea8）、新增500到累计1500，seed5、PPO/ES学习率5e-5。
启动manifest已复核身份/实现指纹、全部DR与观测噪声关闭、nominal参数、预算和
1250/1500独立评估。日志确认实际PPO推进，不只是任务排队；无AMP组风格权重0。
当前仅启动成功，尚未得到本轮正式评估，不能据此宣称抖动/滑动改善或开启DR。
下一步下载唯一编号model_8801250.pt及model_9901250.pt做中期评估，末期对应1500。
普通model_1500.pt保留为后续恢复来源；原模型/运行结果不覆盖，最多剩15个方案轮次。
不创建或恢复旧定时任务。实时状态须再次查询，以上仅为有时间戳的启动快照。
