# AMP 第17/18轮：持续行走的奖励权重对照

## 状态

2026-09-26：第17/18轮均已完成，选择110/control2500作为当前单动作、无DR候选。
它从静止/参考各16个初态独立运行60秒，32/32未跌倒，持续向前，足部节奏接近示范；
提高前进权重的111/progress3仍有1次跌倒，不选用。训练、产物、指标与视频已闭环，
不是仅依据训练奖励或判别器loss判定。当前证据支持固定名义动力学下的AMP可学习性，
不代表低滑移/低抖动已彻底解决，更不代表多动作、域随机化、跨模拟器或真机验收。

独立分支`experiment/f1-amp-sustain`从`018ad95`建立；两组均只追加250次更新。
真实短测108(progress3)/109(control)正常结束，10条更新日志、完整源恢复、
240次实际奖励调用、两类重置和独立回放均核验通过，证书已归档。短测代码`90d008d`。
训练实现指纹`abe8ad2b32856c9fd013f1003e64bf9183d768f8015ce7382d3bfea8b357563b`。
正式110/111分别20:39:14/16启动、20:48:30/20:49:30正常结束，训练代码`e317389`。
两组均从原089/2250恢复，4096×250至2500，未从短测2260模型恢复。
完整CPU回归298项通过，记录`outputs/amp-sustain/cpu-regression-05.log`；
离线分析/日志兼容工具修改没有改变上述训练实现指纹。
正式已完成18/20；不为凑满20轮而继续投入第19/20轮。
后续新试验仍默认32×10短测、4096×250短续训，独立60秒评估不缩短。
DR和观测噪声仍关闭；云端证书不自动放行DR，原数据`training_ready=false`保持不变。

## 本轮证据与假设

输入是第16轮089的全部32个初态60秒确定性评估，包含reference env9在28.95秒的失败。
源模型SHA256为`268a0e93db4fc80592713bdba16fc05174225182f193f666e84aaed5247edb62`。
离线脚本`tools/amp/audit_reward_balance.py`逐项复算任务奖励，并用该最终模型的固定判别器复算AMP奖励。
复算结果与实际环境奖励方法体的CPU测试对照通过，包括dt、两tick门控、软限位和足底速度代理。
真实模型恢复测试逐张量检查策略、状态估计器、判别器、三套优化器和50000窗口经验池。

| reference env9窗口 | 失败前4–2秒 | 最后2秒（含终止帧） |
|---|---:|---:|
| 有效样本数 | 200 | 200 |
| 机身vx m/s | 0.22008 | -0.43703 |
| 前进奖励/控制步 | 0.006570 | -0.006727 |
| 固定最终D的AMP奖励/控制步 | 0.006653 | 0.012663 |
| 稳健加速度惩罚/控制步 | -0.000761 | -0.000197 |
| 足滑惩罚/控制步 | -0.000713 | -0.000024 |
| 动作平滑惩罚/控制步 | -0.000127 | -0.000004 |
| 软位置限位惩罚/控制步 | -0.000837 | -0.002483 |

这些是记录轨迹上的诊断，不是反事实仿真。最终固定D不等于训练中随时间变化的D；
非脚部碰撞力未记录，缺失collision项，因此不能把partial_total_no_collision称为完整回报。
前进奖励在静止时严格为0，不能说机器人停步仍获前进奖励。
停步后某些惩罚变小、AMP奖励却上升，提示任务与风格信号存在局部不一致；
但前进奖励损失仍更大，尚不能证明“惩罚过强”或“D误判”是唯一原因。

可检验假设：在不减弱AMP与平滑/接触约束的前提下，有限增大持续前进信号，可能减少停步。
用同源原奖励续训作为对照，区分权重效应和单纯多训练250次的效应。
新权重不会提高目标速度（仍0.45m/s），不会添加步态相位、关节参考动作或接触标签。

## 唯一干预、预算与验收边界

| 轮次 | 组 | recovery_progress权重 | 其余奖励、AMP与动力学 |
|---|---|---:|---|
| 17 | sustain_control | 2（不变） | 与源模型完全相同 |
| 18 | sustain_progress3 | 3 | 与源模型完全相同 |

- 恢复源仅限`TASK_20260926_089/model_8802250.pt`，完成2250次更新。
- 同一完整学习状态恢复，学习率5e-5，AMP权重1、scale5、39维×10帧不变。
- 先各32环境×10更新短测；真实恢复、奖励调用、两类重置、模型和独立回放均需核验。
- 正式各4096环境×250更新，2250→2500，不从2260短测产物恢复。
- 评估保持60秒、16静止+16参考初态、独立种子5/105；不缩短评估掩盖晚期跌倒。
- 比较全部初态的存活、持续速度、航向、滑移、加速度、动作/力矩差分、冻结D和示范窗口距离及视频。
- 不能用更大的加权训练回报证明更好，不能把减速、停步或早跌带来的低滑移/低加速度当作成功。
- 若生存或风格退化，不能仅凭速度提高选中progress3；效果未通过前继续关闭DR/noise。
- 仅已有账号4409、项目PRO_20260924_005、4090D/ESKU000001、BJX00000001/V000124。
- 不增加账号、不切换资源、不充值；保留原分支、资产、数据与checkpoint。

## 证据入口

- 配置：`configs/amp/lafan_walk02_sustain_{control,progress3}.json`。
- 恢复与运行门禁：`humanoid/amp/sustain.py`；复用真实`X1AMPDirectionEnv`奖励调用。
- 云端产物审计：`tools/amp/verify_direction_{smoke,formal}.py --family sustain`。
- 离线奖励证据：`outputs/amp-sustain/reward-balance089/reward_balance.json`及同目录曲线。
- 本地测试：`tests/test_f1_amp_sustain.py`、`tests/test_amp_reward_balance.py`。
- 云端状态、模型、账单与视频归档于本工作区`outputs/amp-sustain/`，不提交私有运行产物。
- 前16轮结果入口保留在`docs/amp-direction-refinement.md`和它引用的历史文档中。

对照短测首次创建返回平台1002009（创建失败，非余额错误），回查任务列表确认不存在后，
顺序重新提交得到109；未重复创建成功任务，也未切换账号。两组均实际运行66秒，
平台实际各扣费0.09（币种与计费单位字段未给出），已记录本机账号池machineSessions；
20:37余额giftBalance=36.86，cash balance=0，不把训练循环耗时当计费时长。

## 正式结果：短续训有效，加大前进奖励未改善综合表现

全部16初态等权，排除前2秒，失败轨迹保留实际有效前缀、不填零、不隐藏。
32个起步状态的24项已捕获初始字段与源089逐位一致；名义动力学、PD、观测、数据均相同。
每组250条真实更新2251–2500及24,576,000个控制转移完整；精确评估模型与最终模型
的策略/估计器/D/三套优化器/50000窗口缓存逐张量一致。产物审计不等同于效果自动放行。

| 指标（参考起步） | 源089/2250 | 110/control2500 | 111/progress3 |
|---|---:|---:|---:|
| 存活60秒：参考/静止 | 15/16、16/16 | **16/16、16/16** | 15/16、16/16 |
| 机身前向速度m/s | .37215 | .39252 | .43833 |
| 接触足底代表点滑速m/s | .13983 | .12532 | .14185 |
| q二阶差分加速度RMS rad/s² | 54.16 | 50.42 | 57.86 |
| 朝世界X航向RMS度 | 13.68 | 7.62 | 10.14 |
| 最近示范窗口RMS zscore | .64158 | .55099 | .60424 |
| 相同冻结D风格分（非概率） | .65932 | .80429 | .78466 |
| 动作一阶差分RMS | .25545 | .24616 | .26682 |
| 力矩一阶差分RMS Nm | 5.2984 | 5.1676 | 5.5946 |

110相对源模型：足滑下降10.38%、加速度下降6.92%、航向误差下降44.30%、
示范窗口距离下降14.12%，不是靠更早跌倒或减速得到的低成本。目标速度仍.45m/s，
实际.393仍有约.057m/s误差。全部32条的最差1秒滑窗平均机身vx仍为正：
静止组最小.2034m/s、参考组最小.1727m/s（均仅统计2–60秒，未声称每个瞬时速度为正）。

111参考env4在32.55秒后倒。相对110，它更快，但滑速约高13.2%、加速度高14.8%，
且再次跌倒。加权训练奖励变大受progress权重本身影响，不是可比较的质量分数。
本轮选择110，而不是111；当前改善来自同方案再训练250次，不能归功于未采用的权重3。

### 步态节奏、足部交替与残余问题

新增只读工具`tools/amp/audit_gait_rhythm.py`，对示范与全部记录初态统一取根坐标系
左右脚link原点前后位移。镜像关节角的正负号不用于判断交替；接触力阈值抖动也不
直接计作新迈步。离线3Hz零相位低通、峰值间隔/显著度规则均在报告中明确，未给训练
引入步态时钟或相位标签，所有失败前缀保留。

- 示范同侧脚周期频率：左.6309Hz、右.6098Hz（仅6秒示范，各2个完整峰间隔，统计有限）。
- 110参考初态平均：左.6165Hz、右.6169Hz；静止初态.6157/.6174Hz。
- 左右脚前摆极值事件交替率：静止组100%、参考组平均99.63%；相邻极值近同时比例0。
  这是前后摆动交替，不冒称真实接触对的落脚顺序。
- 110左右脚主频相位距理想180°平均偏14.36°，示范偏2.41°，仍有左右时序偏差。
  Welch主频均落在.6667Hz频点，但分辨率只有.1667Hz，不能说实际频率精确相同。
- 接触足底代表点仍有约.125m/s滑速；32条最大几何穿透1.036mm，各episode的
  足部穿透P95最大.163mm。网格几何与足底代表点是明确的代理指标，不是求解器接触对查询。
- 32个起步场景重复用于调参，单训练seed、单6秒示范；这不是未见初态泛化或多种子统计。

因此已观察到持续行走和接近示范的节奏，但不能声称完全复现示范、接触无抖动或足滑为零。
图表/逐条证据：`outputs/amp-sustain/rhythm-audit/{rhythm_report.json,foot_rhythm.png}`。

### AMP对行为的证据及因果边界

重新核对第4/5轮smooth/noamp原始完整日志、模型、恢复链和实际轨迹：二者共同从
060/model1000恢复，环境/动力学/PPO（除run名）一致，仅AMP风格权重1/0不同。
1001–1250的250条加权风格日志已复核，后续各自精确1250恢复到1500的SHA匹配；
两阶段的24项评估初始字段都逐位一致。参考起步的独立示范窗口距离如下：

| 完成更新数 | AMP权重1 | AMP权重0 | 解释 |
|---|---:|---:|---|
| 1250 | .98578 | 1.02180 | AMP更近，但当时AMP组有1次失败，不能只看风格验收 |
| 1500 | 1.09734 | 1.20444 | 两组均16/16存活20秒，AMP组更接近示范 |

这支持该配对续训中的AMP增量风格作用，而不是只看D自己给出的分数。二者均从
AMP预训练模型开始，不能证明“从零不用AMP绝不可能学会”，也不是当前110/2500
的同源消融或多种子显著性结论。原报告和全部失败样本保留，入口为
`docs/amp-dynamic-refine.md`和`docs/amp-refinement-restart.md`，不混同20/60秒评估。

## 视频、曲线与模型

下面相对路径均从本工作树`F:/robot_f1/worktrees/f1-amp-sustain`开始。
MuJoCo技能要求使用真实模型渲染：这里渲染的是独立Isaac Gym策略产生的真实状态，
不是播放示范，不是简化URDF投影，也不是MuJoCo重新执行策略的Sim2Sim验证。

| 内容 | 本地路径 |
|---|---|
| 选择的110：静止起步60秒 | `outputs/amp-sustain/render-110-long60/standing/policy_dual_view.mp4` |
| 选择的110：参考起步60秒 | `outputs/amp-sustain/render-110-long60/reference/policy_dual_view.mp4` |
| 111：两种起步env0各60秒 | `outputs/amp-sustain/render-111-long60/{standing,reference}/policy_dual_view.mp4` |
| 111：失败样本env4，32.55秒 | `outputs/amp-sustain/failure-111-reference-env4/reference/policy_dual_view.mp4` |
| 每段前8秒GIF预览 | 对应MP4同目录`preview_first8s.gif` |
| 主比较曲线及逐条统计 | `outputs/amp-sustain/comparison-long60/` |
| 训练曲线（不可跨权重直接比总奖励） | `outputs/amp-sustain/training-curves/` |
| 111时段分析和失败曲线 | `outputs/amp-sustain/drift-111-long60/` |
| 110精确评估模型 | `outputs/amp-sustain/TASK_20260926_110/model_8802500.pt` |
| 110完整续训模型 | `outputs/amp-sustain/TASK_20260926_110/model_2500.pt` |

五段MP4均完成FFmpeg全文件解码检查：1280×600、50fps；四段3001帧/60.02秒，
失败段1628帧/32.56秒（媒体包含初始帧，物理观测时长分别60/32.55秒）。
已检查代表性中末帧与步态曲线；失败视频明确保留后倒，不仅展示成功env0。
渲染所有帧的key-body FK误差最大5.390e-6m，视觉地板延展只改生成的渲染场景，
没有修改训练资产。未宣称人工逐帧观看完全部视频。

110模型SHA256：`07c0f5b0a0b50fe57b9c42fe5743efad1d4bda9ce806c64be88ea01193446f31`。
110轨迹包SHA256：`de77d4a0c85cd581ae9dc9418d3fbce144730749b9f746f93b9671ec122a833b`。
111模型SHA256：`fd38fa8716373f9b31196ce1d227d65431e9a23c00581d8b0d0e3d1a6a5282b1`。

## 日志异常与账单

两组完整日志各有2次SDK pika连接重置，以及训练完成证书之后的wrapper failed/success
提示；不能说“日志无任何错误”。平台最终状态5、全部250条更新、证书和模型状态均
独立核对完整。111在证书/模型上传成功之后的SDK收尾段含300个NUL和2个非UTF8字节，
独立重新下载哈希相同。原始字节保留，离线工具仅显式允许完成证书之后的尾段转为
可见转义，不忽略错误；完成记录之前/之内的损坏仍拒绝，JSON接口仍严格解析。
日志证书必须与上传manifest完全相等，异常的具体偏移/哈希保存在审计JSON中。

110最终审计`TASK_20260926_110/formal_artifact_audit_v2.json`（v1保留）；
111为`TASK_20260926_111/formal_artifact_audit.json`，位于`outputs/amp-sustain/`。
SDK日志兼容与步态诊断新增测试均通过，不修改训练奖励、模型或旧证书。

20:51实际账单核对：110运行556秒、扣费.90；111运行614秒、扣费.99。
计费runtime字段分别10/11但单位未提供，不等同于墙钟秒数；币种/报价单位也未提供。
两组正式加短测合计实际扣费2.07，giftBalance=34.97、cash balance=0（时间戳快照）。
账号池对应两条machineSessions更新为finished，没有新增账号/充值/换资源/重复任务。
原始计费证据在`F:/agibot_x1_train/work/gradmotion-registration-check/machine-usage-20260926-205147-431.local.json`。
