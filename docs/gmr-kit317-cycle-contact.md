# KIT317 全周期平滑与接触冲击对照

2026-09-12 用户授权按诊断调整并训练，常规实验分支提交、推送、短测和正式启动无需重复询问。
独立分支 `experiment/gmr-kit317-cycle-contact-12dof`，来源 `66be19a`。
原相位实验、原始参考、部署策略不变。本轮不是修改 GMR 原始动作或动力学。

## 实验矩阵

三组均严格恢复原 `TASK_20260911_171/model8000`（内部 iter7999），SHA256：
`4c865c178a12a4796013b6b138972becb875301d7198543a6909c9a76ef7fe50`。
每组 seed5、固定 lr1e-5、33 个网络张量严格恢复、两个优化器清空。
先 512 环境/20 次更新短测，再独立从原 model8000 做 4096 环境/1000 次更新。
只用 1×4090D 24GB；三组最多各一次正式训练，不无限续训、不恢复旧定时任务。

| 任务后缀 | 新全周期加速度 | 接触门控高度解耦 | 新超额接触力 |
| --- | --- | --- | --- |
| cycle_contact | 0 | 开启 | -0.2 |
| cycle_smooth | -0.04 | 保留旧定义 | 0 |
| cycle_both | -0.04 | 开启 | -0.2 |

完整任务名以前缀 `x1_gmr_` 开始。旧1x续训作为历史同预算基线；它不是新增1kHz
协议下的新独立重复。contact 是“门控修复+力幅值约束”这一组修改，不能单独归因到门控。
与 smooth/both 的比较检验接触修改和平滑修改的相互作用，不是多随机种子因果证明。

## 奖励定义

每个1ms物理步刷新速度与接触力，10步累计后给100Hz策略奖励。
`a=(dq_new-dq_old)/0.001`，左右腿各6关节按名称映射。
稳定摆动和稳定支撑包络均只由参考接触标签生成：边界20ms、渐变40ms。
这是30Hz参考标签插值包络，不是毫秒级真实触地标注。
`g=0.25+0.75*max(swing,stance)`，支撑和过渡区不再完全免除新加速度代价。
每腿关节权重 `[1,1,1,1,1.5,2]` 对应髋pitch/roll/yaw、膝、踝pitch/roll。

`C_acc=mean_substeps sum_legs g * mean_6joints(weight*(a/100)^2)`。
保留原 `dof_acc=-1e-7`、动作平滑和100Hz力矩变化率；新项不是替换旧项。
归一化100rad/s²不是免罚阈值，无指数/四次方或硬截断。

从实际机器人刚体质量和重力读取总重BW。每脚力阈值
`allowance=1.5+(0.05-1.5)*swing`（单位BW）；稳定摆动期只容许很小的接触力，
支撑/过渡期容许正常承重，但大冲击仍受罚。`x=max(Fz/BW-allowance,0)`，
`C_force=mean_substeps sum_feet Huber(x)`，其中 x<1 为0.5x²，其余为x-0.5。
线性尾部避免少数巨大冲击按四次方/指数主导；没有封顶或按实际触地关闭门控。
这是力幅值代价，不是冲量测量、安全力限值或物理参数校准。
所有代价仍由基类统一乘控制dt；不改变正奖励、负奖励裁剪、PD或模拟器步进。
系数仅是本轮受控起点，不宣称最优。

## 测量与门禁

记录1kHz全关节、踝侧摆及参考支撑/摆动/过渡加速度，接触力峰值和原始代价。
每组训练结束后才执行两次确定性t0探测：源model8000、该组最终模型。
探测使用训练规模模拟器中的env0，不是原先单环境视频协议；其他环境仍存在，
因此必须明确区分协议，后续可用同一单环境入口再次验收。
保存完整100Hz状态、1kHz速度/加速度/力矩/竖直力/参考门控，捕获在自动重置前。
探测没有学习且发生在训练后，不消耗训练前随机序列；失败也不能冒称完整行走。
`model_cycle_probe.pt` 包含NPZ字节与身份清单，`model_cycle_manifest.pt` 保存配置与指标。
模型、运行日志、视频不进入Git；有效动力学、来源SHA、commit随清单记录。

训练前要求本地单元测试和真实Isaac Gym短测。检查各组身份、对应新增项非零/零、
完整旧配置不变、1kHz采样、无NaN/Inf/OOM，并下载核验短测模型与清单。
本地替身测试只验证数学与调用顺序，不能替代云端动力学验证。
