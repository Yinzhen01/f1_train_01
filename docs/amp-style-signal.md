# AMP 风格信号对照：第 6、7 轮

## 状态与范围

2026-09-24：独立分支 `experiment/f1-amp-style-signal`，基于已发布 `9a3bdd4`。
训练实现 223 项 CPU 全套回归测试通过。
两组云端短测 090/091、正式093/094均完成并下载核验，20秒回放已比较和渲染。
目前1–7轮完成，总体步态尚未验收；
最多 20 轮指实验方案数，而非 PPO 更新数。域随机化、观测噪声继续关闭。
60秒独立回放097/098也已完成；bridge仍有3/32失败，详细结果与下一步见文末。

源数据仍为同一 600 帧、100 Hz WALK_02 左髋 roll ×0.92 候选。
不改变原始数据、机器人资产、PD、物理参数、平滑任务奖励、观测或指令。
不引入步态相位或关节参考跟踪替代 AMP；不以判别器分数上涨作为步态通过。

## 为什么做这两组

此前 smooth+AMP（084）的 1500 更新回放，两种初态均 16/16 完成 20 秒，
但参考初态足滑从 1250 的 0.09823 增到 0.14026 m/s，航向 RMS 从 12.89
增到 34.44°；加速度 RMS 仅从 72.15 降到 70.10 rad/s²。原式将约 39.7%
有效回放窗口截为零风格奖励。独立归一化特征距离也未改善。

判别器能区分示范与策略，可能反映真实的动作分布差距，不等于判别器存在 bug。
只读特征替换诊断表明关节角度/速度差异很大，但替换窗口不一定符合运动学或
动力学，不能作为因果结论、伪示范或训练动作。具体证据集中在
[上一轮统一分析](amp-refinement-restart.md)。

## 严格对照设计

两组都从 TASK_20260924_072 的同一个 `model_8801250.pt` 恢复，实际计数 1250：

`e553b2e800d6c307e746e51f17877e9fd4bf27268979fdd7c3cb2c76453495df`

恢复全部 Actor/Critic/状态估计器、判别器、三个优化器和 50000 条策略缓存。
PPO/状态估计器学习率保持 5e-5，判别器保持 1e-4、每次 2×1024。
仿真与采样从 seed5 新初始化，不宣称恢复源 RNG 或仿真内部状态。
已完成的 084（同源 1250 追加 250）作为未修改对照。

| 方案 | 唯一干预 | 保持不变 |
| --- | --- | --- |
| 第 6 轮 signed | 风格奖励下限由 0 改为 −1，再乘原 dt×scale | 正奖励部分、判别器训练与任务奖励 |
| 第 7 轮 bridge | 额外输入梯度平方正则，系数 1 | 旧零下限奖励、示范 GP=10 与任务奖励 |

signed 使用 `0.01×5×max(−1, 1−(D−1)²/4)`，范围 [−0.05, 0.05]。
目的仅是让原来 D<−1 的一部分窗口不再全部得到相同零奖励；极差窗口仍有
负下限，避免无界惩罚。没有向静止坏动作添加正收益。该改动也会改变收益分布，
可能影响生存倾向，因此必须检查跌倒率，不能只看正负奖励比例。

bridge 在冻结归一化后的示范/负样本特征之间构造混合点；负样本保留原设置，
由真实当前/历史策略窗口与 25% 合成静止姿态窗口组成。alpha 为确定性的
0.05–0.95 分层序列，对判别器输入梯度平方范数施加系数 1 的额外惩罚。
这些点仅是梯度探针，不带示范标签、不进入 replay、不反向传播给策略；
不消耗全局随机数。其有效性是待验证假设，不能靠弱化判别器制造“高分”。

## 门禁、预算和验收

每组先 32 环境×10 更新，1250→1260，包含 reset 和两种初态的独立 1 秒回放。
下载核对模型、轨迹、manifest、10 条训练日志及精确源码指纹。
signed 必须实际出现负风格奖励；bridge 必须实际记录正的额外梯度正则。
训练非有限值、缺失日志、错误恢复源、无效真实历史均拒绝进入正式训练。

通过后每组正式 4096 环境×250 更新，从原 1250 到 1500，不从短测 1260 续训。
不自动延长预算。最终独立 20 秒×16 环境×静止/参考初态，使用同一检查流程：
存活、速度、航向、足滑、关节加速度、高频功率、独立示范特征距离及固定旧判别器。
渲染真实策略状态，固定 env0，不能挑最好案例。比较 084 和原 1250；
奖励变大、程序跑完、无跌倒单项均不代表风格或物理验收。

本机尚无 Isaac Gym。CPU 测试不能代替云端短测。DR 解锁仍为 false。

## 云端短测证据

`4bf2410d41db05a148b3c944ac400a60bf41f9c8`，运行实现指纹
`cb7d88b510893de3061357016759d2b320787483dba508653d20cd62344c4d0f`。
TASK_20260924_090（signed）16:31:33–16:32:39；091（bridge）
16:31:39–16:32:45，均状态5、账号4409、1×4090D、32×10、1250→1260。
每组 7383 个合法窗口、297 个暖启动排除样本、33 次 RSI 初始化，显式 reset
路径及独立 2 环境×两初态×1 秒回放已核实；FK 最大误差 1.586e-6 m。
Actor/D 均更新、全部有限、恢复源/优化器/replay及 no-DR/noise 门禁均通过。
10 条更新日志完整且没有 Traceback/OOM/非有限值。

signed 最后一次更新有效窗口负风格奖励占比 36.46%、零奖励比例0；bridge
额外梯度正则项0.05064、负奖励比例0，确认两项干预分别实际生效，不推断步态改善。
证书见 `docs/validation/signal_{signed,bridge}_cloud_smoke.json`，原始产物在
本分支 ignored 的 `outputs/amp-signal/TASK_20260924_{090,091}/`。

## 正式短续训（2026-09-24 16:43 快照，北京时间）

| 轮次 | 组 | 任务 | 平台接受时间 | 日志已核实累计更新 |
| --- | --- | --- | --- | ---: |
| 6/20 | signed | TASK_20260924_093 | 16:37:26 | 1280 |
| 7/20 | bridge | TASK_20260924_094 | 16:37:38 | 1284 |

两组均使用提交 `e1038011a98247468487a7d2a826fd2d463621f6`，账号4409、
PRO_20260924_005、ESKU000001/1×4090D、BJX00000001/V000124。
最初平台返回状态3但没有日志；直到日志包含真实启动manifest与PPO更新后，
才确认开始训练，不将平台接受时间视为真实PPO起点。

启动manifest已核验：原072/smooth1250精确来源、4096环境、追加250至1500、
源策略/D/三个优化器/50000缓存恢复、实现指纹与短测一致，所有DR及噪声关闭。
本快照未见Traceback；尚未完成或评估，不能推断步态改善，不自动延长。
本地证据 `outputs/amp-signal/TASK_20260924_{093,094}-logs-03.json` 及
对应 status-03/preflight/run-result JSON；当前状态以平台回读为准。

下一步获取精确 `model_8801500.pt`、最终 `model_1500.pt`、独立轨迹
`model_9901500.pt`、`model_amp_manifest.pt` 与完整训练日志。使用
`tools/amp/verify_signal_formal.py` 核对全部250条更新及最终/评估学习状态一致，
再以 `inspect_rollout.py`、同一旧冻结D和统一16初态比较、MuJoCo渲染。
原084、源1250及无AMP085保持只读，不能改旧证书或覆盖旧结果。

## 1500完成与比较（2026-09-24）

093/094均正常状态5，分别结束16:48:49/16:48:50，平台运行683/672秒；其中包含
最初无日志的等待阶段，不将该时长称作纯PPO训练耗时。250条更新1251–1500完整，
最终与评估检查点的策略、判别器、三个优化器和replay均逐张量一致。每组完整日志
有两次仅来自pika通信库的ConnectionResetError；训练完成标记和所有产物完整，
审计逐堆栈严格核对，未把通信异常当成训练失败，也未称日志完全无异常。

下表为全部初态等权、排除前2秒后的描述统计；保留失败初态，不能当多训练seed统计。

| 参考起步指标 | 原公式084 | 负下限093 | 梯度正则094 |
| --- | ---: | ---: | ---: |
| 静止起步20秒存活 | 16/16 | 14/16 | 16/16 |
| 参考起步20秒存活 | 16/16 | 15/16 | 16/16 |
| 体前向速度m/s | .40171 | .36971 | .37058 |
| 接触足底代理滑速m/s | .14026 | .11245 | .09890 |
| 位置差分关节加速度RMS rad/s² | 70.10 | 62.04 | 62.28 |
| 原生速度差分加速度RMS rad/s² | 82.19 | — | 72.81 |
| 航向RMS度 | 34.44 | 46.27 | 20.26 |
| 最近示范窗口RMS zscore | 1.09734 | .83624 | .80366 |
| 同一冻结旧D风格分（非概率） | .29773 | .48450 | .41814 |
| 动作相邻差RMS | .25015 | .21362 | .20745 |
| 力矩相邻差RMS N·m | 5.2833 | 4.8182 | 4.6954 |

093静止env5/6分别在14.71/10.65秒失败，参考env7在19.15秒失败；这些轨迹都保留。
094相对084参考起步足滑−29.49%、加速度−11.15%、航向误差−41.16%、示范距离
−26.76%、>10Hz速度功率占比−10.08%。静止起步也呈类似改善。
但相对较好的源smooth1250，094足滑基本持平（+.69%）、加速度−13.68%、示范
距离−18.48%，参考起步航向反而12.89→20.26度。因此不能表述成所有指标全面改善。

训练末50次更新：原公式D_policy平均−.98566，bridge为−.89745；bridge额外梯度
正则均值.03727。bridge回放零风格奖励窗口22.13%，低于原公式39.71%，且独立
示范距离同步改善，支持该方向值得继续验证，不是只靠把D分数抬高宣布成功。
signed回放约28.43%窗口有负奖励，零截断问题缓解，但航向/存活退化，不优先续训。

源动作审计：600帧平均体前向速度.44964m/s，与指令.45匹配；根yaw净变化2.91度，
范围−.47至12.72度。没有证据把当前20–46度航向RMS解释成源动作要求持续大转弯。
源动作依然只经过运动学检查，不由速度匹配推断动力学可实现。

### 尚未通过的内容

094仍存在接触尖峰、低足部间隙和速度波动。参考/静止回放几何mesh最大负高度
分别约4.54/5.22mm（不是PhysX接触求解器的穿透深度），不能宣称无穿透。
平均速度低于.45m/s，示例静止起步env0的20秒末段速度超过1m/s；20秒未倒不能
代替长期稳定性。原085无AMP续训起点不是同一个bridge1500，不能作为严格配对的
本轮AMP消融。当前effectiveness_verified=false，dr_unlocked=false。

### 产物与下一步

- 完整审计：outputs/amp-signal/TASK_20260924_{093,094}/formal_artifact_audit.json。
- 同口径全部初态比较：outputs/amp-signal/comparison-1500/comparison.json及两张比较图。
- 详细指标：outputs/amp-signal/analysis-{093,094}-1500/report.json。
- 实际状态渲染：outputs/amp-signal/inspection-{093,094}-1500/{standing,reference}/，
  各含policy_dual_view.mp4、preview_first8s.gif、关键帧和曲线。固定env0，不挑最好样本。
  四个视频均1280×600、50fps、1001帧，末帧可解码；GIF均80×100ms，前8秒。
  最大关键刚体FK误差均小于1.75e-6m；已检查中末关键帧及曲线。
  这是MuJoCo渲染真实Gym轨迹，不是MuJoCo动力学/Sim2Real验证。

先以同一084/094模型补60秒、16环境×两初态的只读独立回放，不训练、不增加
实验方案轮次，不提前创建第8/9轮。长回放显式开启extended-validation：
standing/reference分别重置随机种子为5/105，避免前一个模式的失败/reset次数
改变后一个模式的RSI样本。下载后须核实两任务实际初态相同，再做配对比较。
这是明确的新评估协议，不声称与旧20秒参考初态逐位相同；原1/20秒协议不变。
推理适配不改奖励/物理/模型，但会改变源码实现指纹，不能用旧证书启动新源码训练。
长回放入口与明确预算/源SHA/模式种子测试已通过：本地全套227项，48.59秒。
长回放已创建并实际进入推理入口（17:09启动标记核实）：097对应原公式084，
098对应bridge094；平台接受时间17:08:32/36，均账号4409、1×4090D、V000124。
运行代码 `55afa2b8eb34f3f609bbd2c6268a56b9ab55ded0`，输出为各任务的
`model_9961500.pt`；没有PPO/D更新，不计新增训练轮次。17:09快照均状态3，
尚未完成，没有Traceback。后续必须下载真实轨迹，核实两组各模式的实际初态、
全部有效前缀和60秒存活，再分析；不能由入口启动或20秒结果宣布通过长回放。
本地证据outputs/amp-signal/long60-source{084,094}-create-result.json及
TASK_20260924_{097,098}-status-01/logs-01.json。实时平台状态优先于此快照。

## 60秒验证完成：改善存在，但长期稳定性未通过

097/098于17:13:11/13正常状态5结束，未执行PPO或D更新。模型仍为原084/094的
精确1500检查点。两份真实轨迹、完整日志和平台状态均已下载；每份日志各有两次
已核实的SDK pika ConnectionResetError，推理完成标记与产物完整。

`outputs/amp-signal/long60-pair-audit.json`核实：同环境配置、物理步长、PD与DOF属性；
两模式各16初态的root、q、dq、动作、足部刚体与受力等**全部记录字段逐位一致**。
仅说明已观测初态一致，不宣称隐藏PhysX求解状态或多个训练seed等价。
新协议的参考种子为105，不能与旧20秒参考初态逐条混比。

| 60秒，全部初态等权，保留失败前缀 | 原公式084 静止 | bridge094 静止 | 原公式084 参考 | bridge094 参考 |
| --- | ---: | ---: | ---: | ---: |
| 完整存活 | 15/16 | 15/16 | 15/16 | 14/16 |
| 体前向均速m/s | .41612 | .36982 | .41585 | .36176 |
| 接触足滑代理m/s | .14801 | .10275 | .14743 | .10222 |
| 位置差分加速度RMS rad/s² | 71.52 | 63.04 | 71.34 | 62.03 |
| 航向RMS度 | 41.57 | 19.80 | 39.96 | 25.51 |
| 独立示范窗口距离RMS zscore | 1.11518 | .81667 | 1.11256 | .82296 |

参考初态中，bridge相对084足滑约−30.7%、加速度−13.1%、示范距离−26.0%；
但速度离.45m/s更远，存活少1条。不能只依据平滑/风格指标宣布整体更好。
同一冻结D参考分数.28806→.39544，仅辅助指标，不是概率或物理验收。

失败情况：084静止env9于55.41秒、参考env11于38.79秒；bridge静止env14于
36.35秒、参考env10于12.34秒、env11于43.34秒。所有首次失败前缀保留，未拼接重置。
这次bridge参考12.34秒失败不与旧20秒16/16矛盾：两次参考初态协议不同。

### 失败前的信号与推断边界

分段曲线见`outputs/amp-signal/drift-{097,098}-long60/`。bridge静止起步航向RMS
从2–6秒11.31度增到40–60秒23.18度；参考起步16.86→28.29度。后段均值包含较少
初态，报告显式保留n，不能当作所有初态同长度的配对时间趋势。
参考env10在约10.7秒后明显减速、双脚低间隙，然后速度和倾斜迅速增大，最终root
高度降至.285m；另两条失败也表现出末段速度/姿态失控。不能把加速度降低当成
安全：env10末2秒关节加速度43.75，反而低于此前2秒80.60，但它正在跌倒。
脚部净受力有数千牛尖峰，100Hz端点采样不等于完整冲量或唯一碰撞来源，不能直接
推断故障因果或传感器真实冲击。

源码确认当前训练episode每6秒超时重置，而全部这次物理失败发生在6秒以后。
这是**状态覆盖不足的可检验假设**，不是“6秒必然导致跌倒”的证明，也不表示PPO
只能学习6秒或100Hz动作窗口必须改长。AMP局部风格窗口仍保持10帧，不设手工相位。

### 下一轮拟定范围（尚未实现或启动）

第8/9轮拟从同一个bridge094/model_8801500完整恢复，保留奖励、D梯度正则、
优化器、参考、观测和动力学。仅比较训练episode的6秒与60秒上限，固定4096环境，
每组仍只追加250更新到1750；延长单次连续行走不等于增加PPO更新预算。
先重新做32×10真实短测并核实源码指纹，之后才允许正式启动；结尾以相同模式
独立种子和60秒预算评估。若失败/风格继续退化，不机械加时或开启DR。
目前仅完成1–7轮，尚未创建第8/9轮任务。单独接触力约束和严格AMP消融保留为
后续假设，不同时混入这一对照，避免无法区分干预来源。

### 可追溯产物

- 原公式长回放：`outputs/amp-signal/TASK_20260924_097/model_9961500.pt`，SHA256
  `5f85e6347b7fb4fba87baeade4daa99a3f1298d4fa1a7e2295feffa9333650a3`。
- bridge长回放：`outputs/amp-signal/TASK_20260924_098/model_9961500.pt`，SHA256
  `e23a65f064adac0e7e7df1e7669d4a7146c825ef730285c4e82f92ab5de4e69e`。
- 详细分析`analysis-{097,098}-long60/report.json`，统一比较`comparison-long60/`；
  新比较工具按真实duration标注，拒绝把20秒/60秒报告直接合并比较。
- 失败前曲线`drift-098-long60/reference_failure_env10.png`等，保留全部初态总览。
- 视频渲染真实Gym状态，不做MuJoCo动力学。原8m可视地板在长距离行走时移出画面，
  仅新生成的渲染scene按轨迹范围扩大地板；训练资产与物理不变。

最终视频已渲染、完整解码，并检查首/中/末帧；两个固定env0长视频均1280×600、
50fps、3001帧，对应60秒物理轨迹。关键刚体FK误差分别5.033e-6/3.099e-6m。
固定env0两种初态均未倒，不代表全部32条通过。另专门输出最早失败的reference
env10（12.34秒、618帧、FK误差2.534e-6m），不是挑选正常案例替代失败统计。

- [60秒静止起步双视角](../outputs/amp-signal/render-098-long60-widefloor-v2/standing/policy_dual_view.mp4)
- [60秒参考起步双视角](../outputs/amp-signal/render-098-long60-widefloor-v2/reference/policy_dual_view.mp4)
- [最早失败样本完整视频](../outputs/amp-signal/failure-098-reference-env10-v2/reference/policy_dual_view.mp4)
- [失败末段GIF](../outputs/amp-signal/failure-098-reference-env10-v2/reference/failure_last4s.gif)，44帧、4.4秒。

最终采用widefloor-v2目录；旧inspection视频及一次路径类型错误产生的空输出目录
保留为本地过程记录，不作为最终视频。全套230项本地测试通过（52.97秒），
分析/审计/渲染工具提交dbf2af8；云端推理仍是55afa2b，不混淆两种代码身份。
没有增加训练轮次，没有解锁DR。
