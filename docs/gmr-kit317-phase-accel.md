# KIT317：摆动期触地门控修正与相位化加速度惩罚

## 状态与范围

2026-09-12：用户要求实现此前确认的门控修正和相位化加速度惩罚。
独立 worktree：`F:\robot_f1\x1-training-gmr-phase`。
分支：`experiment/gmr-kit317-phase-accel-12dof`，来源 `bbefd04`。
新注册训练环境：`x1_gmr_phase_accel`。

用户已批准提交推送新分支并进行云端短测，明确选择三组实验前的 model8000。
已增加仅允许 512 环境 / 20 次更新 / seed5 的严格恢复入口
`humanoid/scripts/train_gmr_phase_smoke.py`，复用已验证的模型身份校验，不允许长训练。
源为 `TASK_20260911_171/model_8000.pt`，内部 iter7999，SHA256
`4c865c178a12a4796013b6b138972becb875301d7198543a6909c9a76ef7fe50`。
完整网络恢复、优化器重置，固定学习率 1e-5，目标 model8020。
发布/云端运行结果以本文件后续记录为准，此处不是成功声明。
原加速度 1x/3x/10x 分支及运行产物保持不变，未替换部署策略。
本机没有 Isaac Gym，**未做 Isaac Gym 运行验证**。

## 1. 拆开离地高度门控和禁止触地门控

旧定义同时使用参考相位和参考足部最低点高度：

`clearance_gate = phase_gate * height_gate * valid`

`contact_gate = clearance_gate`

新环境通过 `swing.contact_phase_only=True` 显式选择：

`contact_gate = phase_gate * valid`

离地高度惩罚仍使用原来的 `clearance_gate`，不更改参考运动、几何、阈值或高度目标。
左右脚各自使用参考 `foot_contact` 构造摆动包络，**不根据实际触地或实际脚高关闭门控**。
因此策略不能通过提前落脚来逃掉这项惩罚。接触判断依旧为垂直力 `>5 N`，权重仍为 `-1`。
边界排除 `.04 s`、平滑过渡 `.06 s` 不变；这是对 30 Hz 参考采样包络的插值，
不是精确的物理碰撞事件检测。计划触地/离地附近保持过渡容差，而非整段取消监督。

`gmr_swing_contact_gate_mean` 新增为独立诊断；意外触地比例的分母改用对应的接触门控。
旧任务没有启用开关，保留原奖励定义和原分母数值，便于复现旧实验。
新旧门控覆盖范围不同，不能把两种定义的接触比例直接当作行为改善幅度。

### 原轨迹回归

读取原 `x1-training-gmr-accel/outputs/policy_videos/gmr_accel_1x_model9000/rollout.npz`，
使用 episode 0、原始参考与校验过的足底碰撞网格重新计算：

| t≈2.25 s 的左脚 | 原定义 | 新定义 |
| --- | --- | --- |
| 参考接触标签 | 0（摆动） | 不变 |
| 参考最低点高度 | 2.238 mm | 不变 |
| 实际垂直力 | 6528.997 N | 不变，离线轨迹未被重新仿真 |
| 禁止触地门控 | 0 | 1 |
| 接触原始代价 | 0 | 1 |

整个 episode 中，24 个“控制时刻×脚”的触地样本从零门控变为非零门控；
原始离地高度代价逐元素保持一致。此数字只说明旧漏罚样本现在被覆盖，
**不是触地次数、冲击力或抖动下降的训练结果**。

## 2. 在物理步累计相位化加速度平方

物理步长 `h=0.001 s`，每个策略周期有 `D=10` 步，控制周期 `Δt=0.01 s`。
每次刷新 DOF 状态后计算实际关节角加速度：

\[
a_{j,k}=\frac{\dot q_{j,k}-\dot q_{j,k-1}}{h}
\]

新增原始代价为：

\[
C_{\mathrm{phase}}=
\frac{1}{D}\sum_{k=1}^{D}\sum_{f\in\{L,R\}}
g_{f,k}\frac{1}{6}\sum_{j\in J_f}
\left(\frac{a_{j,k}}{100\ \mathrm{rad/s^2}}\right)^2
\]

其中 `J_f` 为该侧六个腿关节，按名称映射；`g` 为参考稳定摆动包络。
每个物理区间取起、止参考门控的较小值，避免跨越排除边界时突然强罚。
相位时间包含每个环境自己的随机参考起点；不使用实际接触作为加速度门控。

初始系数为 `rewards.scales.gmr_phase_acc=-0.02`，实际加到总奖励中的是
`-0.02 * Δt * C_phase`。`scale_rad_s2=100` 只是单位归一化，不是免罚阈值，
也没有指数放大、四次方或硬截断。在恒定加速度、门控为 1 时，新项相当于
每个摆动腿关节增加约 `-3.33e-7 * a²` 的 dt 前系数；但一般运动下不能把它
与 100 Hz 端点加速度奖励简单按系数相比。

该系数是待短测的起点，不是已验证最优值。正常摆腿也需要加速度，因此
必须检查跟踪、抬脚和前进位移，不能仅以加速度代价下降验收。

保留原全相位 `dof_acc=-1e-7`，支撑期仍有弱监督；动作平滑、力矩变化率、
接触防滑、位置/姿态跟踪等奖励全部保持原系数。未改 PD、滤波、延迟、
动作/观测、网络、URDF、armature、参考轨迹或 DR。

### 为什么在 1 kHz 累计

一个 10 ms 控制周期内速度先增后减，端点可以相同，旧端点差分会给出零加速度。
逐个 1 ms 步求平方再平均不会正负抵消。它仍是每个物理步的离散平均加速度，
不是连续时间峰值测量；运行时要求保持 `sim.substeps=1`。

基类只添加两个默认空的采样钩子，不改变旧任务的物理/控制流程；新环境在
DOF 状态刷新后采样。每个控制周期重置累计器，历史速度从当前状态复制，
重置后首个周期不启用新惩罚。奖励求和前必须已经收齐十个样本，否则报错。

新增日志保留门控下左右腿 RMS、全物理步 RMS/峰值、门控占比、原始代价及
dt 前加权代价；旧 100 Hz 指标保留原名称，避免混淆采样率。

## 3. 验证与未闭环项

本地 Python 3.8 / Torch 2.4.1 CPU：

- `python -m unittest discover -s tests -v`：118 项通过，其中本分支新增 18 项。
- 修改的 Python 文件语法检查通过；`git diff --check` 通过。
- 测试覆盖真实参考相位、原漏罚点、左右腿名称映射、短摆动和边界、随机起点、
  重置历史、正负加速度不抵消、重复/缺失采样报错、运行频率防误配。
- 模拟器替身执行真实基类 `step` 方法，验证十次状态刷新后采样、奖励缓存先于求和，
  以及采样开关不修改动作、力矩与状态；这不是 Isaac Gym 动力学测试。

限制与下一步门禁：

1. 接触力仍按原 100 Hz 控制端点采样；新增 1 kHz 采样目前仅用于 DOF 加速度。
   不能声称捕获了所有短于控制周期的触地脉冲。
2. 参考本身过低/相位不合适仍可能造成跟踪与禁止触地冲突。本轮先修复监督缺口，
   不暗中修改重定向数据；后续需要比较脚高不足、接触比例、足滑及跟踪误差。
3. 本地没有 Isaac Gym。正式长训练前必须经授权发布，并在云端做短 smoke，
   检查运行时采样率、有效奖励、额外开销、NaN/Inf 和正常摆腿是否受抑。
4. 短测必须使用 `train_gmr_phase_smoke.py`：严格绑定用户选定的 model8000、
   模型/参考/URDF 哈希和云端 commit，检查旧配置未被改变。不要直接使用
   通用 train.py 随机初始化来冒充续训，也不要使用三组旧权重的训练入口。
5. 后续续训须记录经确认的源任务、模型 SHA、代码 commit 和预算，验证完整网络
   恢复并使用新优化器，再同协议推理比较。118 项本地测试不等于抖动已改善。

主要实现：`humanoid/gmr_phase_accel.py`、`humanoid/envs/x1/x1_gmr_phase_accel_env.py`、
`humanoid/envs/x1/x1_gmr_phase_accel_config.py`；回归测试：`tests/test_gmr_phase_accel.py`。
